-- Cargamos una librería de interfaz de usuario (ejemplo: Rayfield UI)
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- Creamos la ventana principal
local Window = Rayfield:CreateWindow({
   Name = " Hub Scripts | BunnyFreeScripts",
   LoadingTitle = "LOADING...",
   LoadingSubtitle = "BUNNYFREESCRIPTS",
   ConfigurationSaving = { Enabled = false }
})

-- Creamos una pestaña para los scripts de minijuegos / farm
local MainTab = Window:CreateTab("📜 Lista de Scripts", 4483362458) -- Icono opcional

-- BOTÓN 1: SCRIPT DE MIEL
local Button1 = MainTab:CreateButton({
   Name = "🍯 HONEY COLLECTOR CHOCOLA ",
   Callback = function()
       -- Aquí pones el link de tu script de Miel
       loadstring(game:HttpGet("https://raw.githubusercontent.com/chocolascript-glitch/Chocola-Auto-Honey/refs/heads/main/script.lua"))()
       
       Rayfield:Notify({
          Title = "EXECUTED",
          Content = "EXECUTED.",
          Duration = 3.5,
       })
   end,
})

-- BOTÓN 2: SCRIPT DE RULETA / AUTOSPIN
local Button2 = MainTab:CreateButton({
   Name = "🎰 Autospin RNG",
   Callback = function()
       -- Aquí ejecutas tu script de autospin
       loadstring(game:HttpGet("https://raw.githubusercontent.com/chocolascript-glitch/Chocola-Auto-Spin-RNG/refs/heads/main/script.lua"))()
       
       Rayfield:Notify({
          Title = "EXECUTED",
          Content = "EXECUTED.",
          Duration = 3.5,
       })
   end,
})

-- BOTÓN 2: SCRIPT DE RULETA / AUTOSPIN
local Button3 = MainTab:CreateButton({
   Name = "SERVERHOPPER FOR AUTOHONEY",
   Callback = function()
       -- Aquí ejecutas tu script de autospin
       loadstring(game:HttpGet("https://pastefy.app/sFOkaUno/raw"))() 
       
       Rayfield:Notify({
          Title = "EXECUTED",
          Content = "EXECUTED.",
          Duration = 3.5,
       })
   end,
})

-- BOTÓN 4: SCRIPT DE RULETA / AUTOSPIN
local Button4 = MainTab:CreateButton({
   Name = "AUTOBUY BEE SHOP",
   Callback = function()
       -- Aquí ejecutas tu script de autospin
       loadstring(game:HttpGet("https://pastefy.app/FLOSU5Pk/raw"))()
       
       Rayfield:Notify({
          Title = "EXECUTED",
          Content = "EXECUTED.",
          Duration = 3.5,
       })
   end,
})

-- BOTÓN 4: SCRIPT DE RULETA / AUTOSPIN
local Button4 = MainTab:CreateButton({
   Name = "AUTOCOLLECT HONEY KY",
   Callback = function()
       -- Aquí ejecutas tu script de autospin
       loadstring(game:HttpGet("https://pastefy.app/wdEAoCOz/raw"))()
       
       Rayfield:Notify({
          Title = "EXECUTED",
          Content = "EXECUTED.",
          Duration = 3.5,
       })
   end,
})
